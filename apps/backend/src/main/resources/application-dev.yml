spring:
  application:
    name: kisan-sarathi-backend

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/krishi}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:admin}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  rabbitmq:
    host: horse.lmq.cloudamqp.com
    port: 5671
    username: rqkagovn
    password: oqqdaXLuGSjLDhC8oyPcgtFvOKmGE5ha
    virtual-host: rqkagovn
    ssl:
      enabled: true
      algorithm: TLSv1.2

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8089}
  error:
    include-message: always
    include-binding-errors: always

# Application Configuration
app:
  jwt:
    secret: ${JWT_SECRET:dGhpc2lzYXZlcnlzZWNyZXRrZXlmb3JrcmlzaGlodWJuZXBhbGFwcGxpY2F0aW9uMjU2Yml0cw==}
    expiration: 3600000 # 1 hour in milliseconds
    refresh-expiration: 3600000 # 1 hour in milliseconds

  otp:
    expiration: 300 # 5 minutes in seconds
    length: 6

  cloudinary:
    cloud-name: ${CLOUDINARY_CLOUD_NAME:dsqubrfjp}
    api-key: ${CLOUDINARY_API_KEY:624365773875321}
    api-secret: ${CLOUDINARY_API_SECRET:9P5wS1lelWdo6CubuJxzQiNTClw}

  sms:
    api-url: ${SMS_API_URL:}
    api-key: ${SMS_API_KEY:}
    sender-id: ${SMS_SENDER_ID:KISAN}

  payment:
    esewa:
      merchant-code: ${ESEWA_MERCHANT_CODE:EPAYTEST}
      secret-key: ${ESEWA_SECRET_KEY:8gBm/:&EnhH.1/q}
      base-url: ${ESEWA_BASE_URL:https://rc-epay.esewa.com.np/api/epay/main/v2/form}
      verify-url: ${ESEWA_VERIFY_URL:https://rc-epay.esewa.com.np/api/epay/transaction/status/}
      success-url: ${ESEWA_SUCCESS_URL:http://localhost:8080/payment/success}
      failure-url: ${ESEWA_FAILURE_URL:http://localhost:8080/payment/failure}
    khalti:
      public-key: ${KHALTI_PUBLIC_KEY:}
      secret-key: ${KHALTI_SECRET_KEY:}
      success-url: ${KHALTI_SUCCESS_URL:http://localhost:5173/payment/success}
      failure-url: ${KHALTI_FAILURE_URL:http://localhost:5173/payment/failure}

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8080,https://krishi-bazaar-nepal.vercel.app/,http://localhost:8081}
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS
    allowed-headers: "*"
    allow-credentials: true




logging:
  level:
    com.krishihub: DEBUG
    org.springframework.web: INFO
    org.hibernate: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

weather:
  ingestion:
    cron: "0 */5 * * * *" # Every 5 minutes
  openweathermap:
    api-key: cc1f6ee6c3d30d4276544a101c0607ad
    base-url: https://api.openweathermap.org/data/2.5
